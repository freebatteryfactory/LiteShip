{
  "$comment": "Owner sign-offs for the AGENT-SAFETY META-GAUNTLET (the raccoon rule). A weakening of the gauntlet's own rigor standards is permitted ONLY via an explicit, owner-signed entry here. Each sign-off names the EXACT element being weakened (elementKey), the weakening CLASS it authorizes, the owner, a justification, and an expiry (ISO yyyy-mm-dd; past the run date re-reds). An always-blocking weakening (the placeholder/skip floor) can NEVER be signed (void), and a skip-allowlist-add whose SITE carries a placeholder marker (TODO/FIXME/not implemented/stub/...) is non-signable (it is unfinished work, not a capability gate). The 29 entries below include the branch-local weakenings vs the snapshot's birth commit (d0c214b7) plus Tier 0.2 skipIf conversions (offscreen-canvas, webcodecs, gpu-absent, eacces-untestable-as-root, symlink-unprivileged, fixture-absent, capsule-manifest-absent) plus two line-relocations of existing benign waivers (clock.ts no-nondeterminism 60/77; version.ts no-silent-catch 47→46 from the Wave 5 runCliCommand migration). Derived from the live-vs-birth diff, not hand-guessed.",
  "signoffs": [
    {
      "elementKey": "skip-allowlist::tests/browser/spsc-ring-browser.test.ts::describe.skipIf(!canUseSAB)('browser SPSCRing with real SharedArrayBuffer and Atomics', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (SharedArrayBuffer / cross-origin isolation (COOP/COEP)). describe.skipIf(!canUseSAB) — the real-SharedArrayBuffer SPSCRing test BODY runs when cross-origin isolation is present and skips only when absent (the in-process ring is covered elsewhere); a runtime capability gate, not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/e2e/astro-directives.e2e.ts::test.skip(!built, 'astro example not built — run: pnpm exec tsx tests/integration/astro/test.ts');",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (the built Astro example dist). test.skip(!built, …) — the browser e2e BODY runs when the built Astro example dist is present; the gauntlet runs the integration build before the e2e lane, so CI always exercises it. A runtime capability gate, not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/integration/cli/idempotency.test.ts::const renderIt = FFMPEG_RENDER_CAPABLE ? it : it.skip;",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (ffmpeg + libx264 on PATH). FFMPEG_RENDER_CAPABLE ? it : it.skip — the render-idempotency BODY runs when ffmpeg+libx264 is on PATH and skips only when absent (the host context unit covers the encode path). A runtime capability gate, not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/integration/cli/scene-dev.test.ts::const conditionalIt = underCoverage ? it.skip : it;",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (absence of v8 coverage instrumentation). underCoverage ? it.skip : it — the spawned tsx→vite-server BODY runs except under v8 coverage (a coverage-vs-vite STATUS_ACCESS_VIOLATION on Windows); the in-process server.test.ts covers the same startDevServer() path under coverage. A runtime instrumentation gate, not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/integration/cli/scene-render.test.ts::const renderIt = FFMPEG_RENDER_CAPABLE ? it : it.skip;",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (ffmpeg + libx264 on PATH). FFMPEG_RENDER_CAPABLE ? it : it.skip — the scene-render CLI BODY renders an mp4 through ffmpeg+libx264 when the codec is present and skips only when absent. A runtime capability gate, not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/property/boundary-evaluate-batch.prop.test.ts::describe.skipIf(!wasmPresent)('Boundary.evaluateBatch agrees with scalar evaluate (Rust kernel loaded)', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (the loaded Rust WASM kernel). describe.skipIf(!wasmPresent) — the Boundary.evaluateBatch parity property BODY runs when the Rust kernel is loaded and skips only when absent (the scalar evaluate path is covered without it). A runtime capability gate, not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/smoke/intro-render.test.ts::it.skip('skipped — ffmpeg libx264 render probe failed (see liteship doctor)', () => {});",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (ffmpeg + libx264 on PATH). the end-to-end intro-scene smoke render BODY runs through ffmpeg+libx264 when present; absent the codec the doctor-probe skips (see `liteship doctor`). The title names the capability (render probe failed), never a TODO — a runtime capability gate, not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/command/error-contract.test.ts::it.runIf(FFMPEG_RENDER_CAPABLE)('a stream failure mentioning stdin/EPIPE surfaces the probe verdict', async () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (ffmpeg + libx264 on PATH). it.runIf(FFMPEG_RENDER_CAPABLE) — the stream-failure probe (EPIPE/stdin) BODY runs only when ffmpeg+libx264 is capable. A runtime capability gate (runIf on the real render backend), not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/command/ffmpeg-render-backend.test.ts::test.skip('ffmpeg+libx264 render (skipped — codec not on PATH)', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (ffmpeg + libx264 on PATH). the real ffmpeg+libx264 render backend BODY runs when the codec is on PATH and skips only when absent. The title names the capability (codec not on PATH), never a TODO — a runtime capability gate, not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/command/host-context.test.ts::it.runIf(FFMPEG_RENDER_CAPABLE)('renderScene encodes frames through ffmpeg when libx264 is available', async () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (ffmpeg + libx264 on PATH). it.runIf(FFMPEG_RENDER_CAPABLE) — renderScene-through-ffmpeg BODY runs only when libx264 is available. A runtime capability gate (runIf on the real backend), not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/core/wasm-parity.test.ts::describe.skipIf(!wasmPresent)('WASM/TS kernel parity (liteship-compute vs fallbackKernels)', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (the loaded Rust WASM kernel). describe.skipIf(!wasmPresent) — the WASM/TS kernel parity arm BODY runs when the Rust kernel IS loaded (compares liteship-compute against fallbackKernels). A runtime capability gate, not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/core/wasm-parity.test.ts::describe.skipIf(wasmPresent)('WASM/TS kernel parity (artifact absent — fallbackKernels is what ships here)', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (the wasm artifact being ABSENT). describe.skipIf(wasmPresent) — the INVERSE parity arm BODY runs when the wasm artifact is ABSENT (the fallback is what ships there). A runtime capability gate, not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/core/wasm-shipping.test.ts::it.skipIf(!staged)('resolves @liteship/core dist/liteship-compute.wasm via the module graph', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (the staged @liteship/core dist wasm artifact). it.skipIf(!staged) — the module-graph resolution of @liteship/core dist/liteship-compute.wasm BODY runs only when the built artifact is staged (a publish-shape probe). A runtime capability gate, not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/stage/dual-export-node.test.ts::test.skip('headless dual-export end to end (skipped — codec not on PATH)', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (ffmpeg + libx264 on PATH). the headless dual-export end-to-end BODY runs when the ffmpeg+libx264 codec is on PATH and skips only when absent. The title names the capability (codec not on PATH), never a TODO — a runtime capability gate, not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/stage/ffmpeg-encoder.test.ts::test.skip('ffmpeg+libx264 encode (skipped — codec not on PATH)', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Sanctioned capability-gated skip (ffmpeg + libx264 on PATH). the real ffmpeg+libx264 encode BODY runs when the codec is on PATH and skips only when absent. The title names the capability (codec not on PATH), never a TODO — a runtime capability gate, not a placeholder. Enumerated in SANCTIONED_SKIPS at site granularity; signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/browser/render-worker-browser.test.ts::describe.skipIf(offscreenCanvasAbsent)('OffscreenCanvas-dependent flows', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Tier 0.2 — converted silent early-return to describe.skipIf. OffscreenCanvas-dependent browser worker tests BODY runs when OffscreenCanvas is defined; skips only when absent (a runtime capability gate, not a placeholder). Replaces a lying early-return pass.",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/browser/webcodecs-capture.test.ts::describe.skipIf(webcodecsAbsent)('WebCodecsCapture — browser lane', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Tier 0.2 — capability-gated skip for WebCodecs browser lane. describe.skipIf(webcodecsAbsent) — the VideoEncoder/VideoFrame BODY runs when WebCodecs is available; skips only when absent (a runtime capability gate linked to the canonical webcodecsAbsent probe, not a placeholder).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/astro/wgpu-runtime.test.ts::it.skipIf(gpuAbsent)(",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Tier 0.2 — converted silent early-return to it.skipIf. WebGPU runtime tests BODY runs when navigator.gpu is present; skips only when absent (a runtime capability gate, not a placeholder). Replaces a lying early-return pass.",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/cli/lib/gauntlet-verdict-cache.test.ts::it.skipIf(eaccesUntestableAsRoot)('a cache file with the read bit cleared (EACCES) reads as a MISS, not a throw', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Tier 0.2 — converted silent early-return to it.skipIf. EACCES permission test BODY runs only when chmod 000 is testable (non-root); skips on root where EACCES cannot be provoked (a runtime capability gate, not a placeholder).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/cli/lib/gauntlet-verdict-cache.test.ts::it.skipIf(eaccesUntestableAsRoot)('a mutation cache file with the read bit cleared (EACCES) reads as a MISS, not a throw', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Tier 0.2 — converted silent early-return to it.skipIf. Mutation-cache EACCES test BODY runs only when chmod 000 is testable (non-root); skips on root where EACCES cannot be provoked (a runtime capability gate, not a placeholder).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/devops/audit-consumer-mode.test.ts::it.skipIf(symlinkUnprivileged)('resolves the pnpm virtual-store layout via realpath re-seeding', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Tier 0.2 — converted silent symlink-privilege return to it.skipIf. The pnpm virtual-store realpath BODY runs when directory symlinks are available and skips only on unprivileged symlink hosts; npm-shaped consumer discovery still covers the non-symlink path.",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/meta/generated-bench-execution.test.ts::describe.skipIf(capsuleManifestAbsent)(",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Tier 0.2 — honest smoke-lane skip for the generated-bench corpus proof. The BODY runs when capsule:compile has produced the manifest; lanes without that manifest skip the catalog-derived proof instead of throwing or silently fabricating sources.",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/riff-walker.test.ts::it.skipIf(fixtureAbsent)('decodes the shipped intro-bed.wav fixture cleanly', () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Tier 0.2 — converted fixture-absent early return to it.skipIf. The real RIFF fixture BODY runs when intro-bed.wav is present; synthetic RIFF coverage still runs when the optional asset is absent.",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/vite/boundary-manifest.test.ts::test.skipIf(symlinkUnprivileged)('scan terminates on circular directory symlinks and still derives the right entries', async () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Tier 0.2 — converted circular-directory symlink early return to test.skipIf. The traversal BODY runs when directory symlinks are available and skips only on unprivileged symlink hosts; ordinary boundary scanning remains covered.",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/vite/boundary-manifest.test.ts::test.skipIf(symlinkUnprivileged)('follows symlinked directories to boundary definitions outside the project tree', async () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Tier 0.2 — converted external-definition symlink early return to test.skipIf. The symlinked boundary definition BODY runs when directory symlinks are available and skips only on unprivileged symlink hosts; boundaryDir override coverage remains the non-symlink proof.",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "skip-allowlist::tests/unit/vite/token-manifest.test.ts::test.skipIf(symlinkUnprivileged)('scan terminates on circular directory symlinks and still derives entries', async () => {",
      "weakening": "skip-allowlist-added",
      "owner": "heyoub",
      "justification": "Tier 0.2 — converted token-manifest circular-directory symlink early return to test.skipIf. The traversal BODY runs when directory symlinks are available and skips only on unprivileged symlink hosts; ordinary token manifest coverage remains active.",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "waiver::gauntlet/no-nondeterminism|packages/core/src/clock/clock.ts|60",
      "weakening": "waiver-added",
      "owner": "heyoub",
      "justification": "Same gauntlet/no-nondeterminism boundary waiver (a sanctioned Date.now() entropy boundary at line 60), RELOCATED by the P4 core-domain migration that moved packages/core/src/clock.ts into packages/core/src/clock/clock.ts — NOT a new deferral. The waiver key embeds the file path, so the pure rename surfaces as remove(old path)+add(new path); the corresponding removal of the packages/core/src/clock.ts|60 waiver is a paired strengthen. No additional code is waived; the same line-60 entropy boundary is waived, only its home moved. Signed vs the base-ref baseline (the P4 restructure window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "waiver::gauntlet/no-nondeterminism|packages/core/src/clock/clock.ts|77",
      "weakening": "waiver-added",
      "owner": "heyoub",
      "justification": "Same gauntlet/no-nondeterminism boundary waiver (a sanctioned Date.now() entropy boundary at line 77), RELOCATED by the P4 core-domain migration that moved packages/core/src/clock.ts into packages/core/src/clock/clock.ts — NOT a new deferral. The waiver key embeds the file path, so the pure rename surfaces as remove(old path)+add(new path); the corresponding removal of the packages/core/src/clock.ts|77 waiver is a paired strengthen. No additional code is waived; the same line-77 entropy boundary is waived, only its home moved. Signed vs the base-ref baseline (the P4 restructure window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "waiver::gauntlet/no-silent-catch|packages/cli/src/commands/version.ts|46",
      "weakening": "waiver-added",
      "owner": "heyoub",
      "justification": "Same gauntlet/no-silent-catch benign-catch waiver (readCliVersion's best-effort import.meta.url fall-through in packages/cli/src/commands/version.ts, non-corrupting: a real version is still resolved from the workspace), RELOCATED one line (47→46) by the Wave 5 runCliCommand migration dropping the spawn imports above it — NOT a new deferral. The waiver key embeds the line number, so a one-line shift surfaces as remove(line 47)+add(line 46); the corresponding removal is a strengthen. No additional code is waived (identical to the clock.ts:60/77 relocation precedent). Signed vs the snapshot's birth baseline (the bootstrap window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "waiver::gauntlet/no-silent-catch|packages/cli/src/commands/ship.ts|131",
      "weakening": "waiver-added",
      "owner": "heyoub",
      "justification": "Same gauntlet/no-silent-catch benign-catch waiver (the best-effort workspace-glob enumeration in packages/cli/src/commands/ship.ts — an entry that cannot be statSync-d is skipped during package discovery, non-corrupting and conservative: a genuinely unreadable directory entry is not a publishable package), RELOCATED (169→131) by the Wave 8 effect-shed removing the runEffect adapters above resolveGlob — NOT a new deferral. The waiver key embeds the line number, so the shift surfaces as remove(line 169)+add(line 131); the corresponding removal is a strengthen. No additional code is waived (identical to the version.ts:47→46 and clock.ts:60/77 relocation precedents). Surfaces as a weakening vs origin/main now that the base ref carries the snapshot (the gate moved from birth-baseline to origin/main baseline).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "waiver::gauntlet/no-silent-catch|packages/cli/src/commands/doctor/probes-workspace.ts|261",
      "weakening": "waiver-added",
      "owner": "heyoub",
      "justification": "Same gauntlet/no-silent-catch benign-catch waiver (the best-effort playwright-browsers cache probe in hasChromiumBuild — an unreadable browsers-cache dir reads as \"no chromium installed\", the conservative non-corrupting interpretation the doctor surfaces to the user exactly like its normal output), RELOCATED (250→261) by the P9 doctor spawn-capture seam adding the injectable SpawnArgvCapture params above hasChromiumBuild — NOT a new deferral. The waiver key embeds the line number, so the shift surfaces as remove(line 250)+add(line 261); the corresponding removal is a strengthen. No additional code is waived (identical to the ship.ts:169→131 and version.ts:47→46 relocation precedents). Surfaces as a weakening vs origin/main now that the base ref carries the snapshot (the gate moved from birth-baseline to origin/main baseline).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "waiver::gauntlet/no-nondeterminism|packages/core/src/internal/rng.ts|39",
      "weakening": "waiver-added",
      "owner": "heyoub",
      "justification": "Same gauntlet/no-nondeterminism entropy-boundary waiver (the sanctioned seeded-RNG entropy source at line 39), RELOCATED by the P4 core-domain migration that moved packages/core/src/rng.ts into packages/core/src/internal/rng.ts — NOT a new deferral. The waiver key embeds the file path, so the pure rename surfaces as remove(packages/core/src/rng.ts|39)+add(packages/core/src/internal/rng.ts|39); the removal is a paired strengthen. No additional code is waived; the same line-39 entropy boundary is waived, only its home moved. Signed vs the base-ref baseline (the P4 restructure window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "assurance::packages/core/src/{receipt,hlc,plan,dag,validated-output,assembly,brands}.ts",
      "weakening": "assurance-level-lowered",
      "owner": "heyoub",
      "justification": "Glob-membership RESTRUCTURE, not a rigor loss: the P4 core-domain migration split the flat L4 group {receipt,hlc,plan,dag,validated-output,assembly,brands}.ts into the domain-homed L4 globs it now lives under — evidence/{receipt,validated-output}.ts, clock/hlc.ts, authoring/{plan,assembly}.ts, graph/dag.ts, schema/brands.ts (each surfaced as a paired ADD strengthen in the same run). Every path KEPT its L4 assurance band; no file dropped a rigor level. The waiver key embeds the exact glob string, so relocating the files surfaces as remove(old flat glob)+add(new subdir globs). Net rigor unchanged. Signed vs the base-ref baseline (the P4 restructure window).",
      "expiry": "2027-06-20"
    },
    {
      "elementKey": "assurance::packages/core/src/{boundary,signal,zap,evaluate,gen-frame,speculative,token-buffer,blend,animation,ai-cast,clock,rng}.ts",
      "weakening": "assurance-level-lowered",
      "owner": "heyoub",
      "justification": "Glob-membership RESTRUCTURE, not a rigor loss: the former L3 group {boundary,signal,zap,evaluate,gen-frame,speculative,token-buffer,blend,animation,ai-cast,clock,rng}.ts was SPLIT — signal.ts moved into the new reactive-core group {cell-kernel,cell,derived,store,signal,timeline,live-cell}.ts at L4 (a PROMOTION L3→L4; see assurance-map.ts: 'signal moves L4 from its former L3 … a CellKernel-backed value channel, not a bespoke source') plus a new mutation-score floor, and the remaining paths stay L3 under the renamed glob {boundary,zap,evaluate,gen-frame,speculative,token-buffer,blend,animation,ai-cast,clock,rng}.ts (surfaced as the paired ADD strengthen). The waiver key embeds the exact glob string, so editing group membership surfaces as remove(old glob)+add(new glob) — no path lost its rigor band; net rigor INCREASED. Surfaces as a weakening vs origin/main now that the base ref carries the snapshot (the gate moved from birth-baseline to origin/main baseline).",
      "expiry": "2027-06-20"
    }
  ]
}
