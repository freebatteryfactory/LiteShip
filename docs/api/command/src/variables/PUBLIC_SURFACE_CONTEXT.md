[**LiteShip**](../../../README.md)

***

[LiteShip](../../../modules.md) / [command/src](../README.md) / PUBLIC\_SURFACE\_CONTEXT

# Variable: PUBLIC\_SURFACE\_CONTEXT

> `const` **PUBLIC\_SURFACE\_CONTEXT**: `object` = `GENERATED_PUBLIC_SURFACE_CONTEXT`

Defined in: [command/src/commands/public-surface-context.ts:156](https://github.com/freebatteryfactory/LiteShip/blob/main/packages/command/src/commands/public-surface-context.ts#L156)

The complete generated projection returned by `liteship describe`.

## Type Declaration

### lifecycle

> `readonly` **lifecycle**: readonly \[\{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"gc-owned-mutable"`; `disposal`: `"none"`; `operation`: `"RuntimeCoordinator.create"`; `owner`: `"@liteship/core/reactive"`; `postDispose`: `"not-applicable"`; `proof`: `"tests/unit/core/reactive/runtime-coordinator.test.ts"`; `rationale`: `"Bounded in-memory stores own no timer, subscription, process, or external handle."`; `remediation`: `"Do not add artificial disposal; Bounded in-memory stores own no timer, subscription, process, or external handle. Verify tests/unit/core/reactive/runtime-coordinator.test.ts."`; `siblingCleanup`: `"not-applicable"`; `specifier`: `"liteship/reactive"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"gc-owned-mutable"`; `disposal`: `"none"`; `operation`: `"StateCellStore.create"`; `owner`: `"@liteship/core/reactive"`; `postDispose`: `"not-applicable"`; `proof`: `"tests/unit/core/reactive/state-cell.test.ts"`; `rationale`: `"The registry is an in-memory authority over a coordinator and starts no background work."`; `remediation`: `"Do not add artificial disposal; The registry is an in-memory authority over a coordinator and starts no background work. Verify tests/unit/core/reactive/state-cell.test.ts."`; `siblingCleanup`: `"not-applicable"`; `specifier`: `"liteship/reactive"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createCell"`; `owner`: `"@liteship/core/reactive"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/core/reactive/cell.test.ts"`; `rationale`: `"The cell owns subscriptions and exposes its Lifetime directly through dispose."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/core/reactive/cell.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/reactive"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"gc-owned-mutable"`; `disposal`: `"none"`; `operation`: `"createDirtyFlags"`; `owner`: `"@liteship/core/reactive"`; `postDispose`: `"not-applicable"`; `proof`: `"tests/unit/core/reactive/dirty-flags-runtime.test.ts"`; `rationale`: `"Dirty flags are bounded synchronous bit storage with no active resource."`; `remediation`: `"Do not add artificial disposal; Dirty flags are bounded synchronous bit storage with no active resource. Verify tests/unit/core/reactive/dirty-flags-runtime.test.ts."`; `siblingCleanup`: `"not-applicable"`; `specifier`: `"liteship/reactive"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createLifetime"`; `owner`: `"@liteship/core/reactive"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/core/reactive/lifetime.test.ts"`; `rationale`: `"Lifetime is the aggregate exactly-once finalizer authority."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/core/reactive/lifetime.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/reactive"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createLiveCell"`; `owner`: `"@liteship/core/reactive"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/core/reactive/disposed-write-inert.test.ts"`; `rationale`: `"The live cell owns its reactive kernel and closes writes and subscribers on disposal."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/core/reactive/disposed-write-inert.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/reactive"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createLiveCellBoundary"`; `owner`: `"@liteship/core/reactive"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/core/reactive/disposed-write-inert.test.ts"`; `rationale`: `"The boundary live cell owns its reactive kernel and crossing stream."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/core/reactive/disposed-write-inert.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/reactive"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createQuantizer"`; `owner`: `"@liteship/quantizer"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/quantizer/animated-quantizer.test.ts"`; `rationale`: `"A live quantizer owns input subscriptions, transition work, and output delivery."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/quantizer/animated-quantizer.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/reactive"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createSignal"`; `owner`: `"@liteship/core/reactive"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/core/reactive/signal.test.ts"`; `rationale`: `"A signal owns source listeners and its delivery kernel."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/core/reactive/signal.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/reactive"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createStore"`; `owner`: `"@liteship/core/reactive"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/core/reactive/store.test.ts"`; `rationale`: `"A store owns subscribers and reducer delivery through one Lifetime."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/core/reactive/store.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/reactive"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createBlendTree"`; `owner`: `"@liteship/core/motion"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/core/motion/blend-tree.test.ts"`; `rationale`: `"The blend tree owns its change kernel and derived subscriptions."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/core/motion/blend-tree.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/motion"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createTimeline"`; `owner`: `"@liteship/core/motion"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/liteship/facade-lifecycle-contract.test.ts"`; `rationale`: `"The timeline owns its scheduler and state delivery kernel through direct disposal."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/liteship/facade-lifecycle-contract.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/motion"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"pure-allocation"`; `disposal`: `"none"`; `operation`: `"createGraphMutationClient"`; `owner`: `"@liteship/core/graph"`; `postDispose`: `"not-applicable"`; `proof`: `"tests/unit/core/graph/graph-mutation-client.test.ts"`; `rationale`: `"The client allocates request closures but owns no persistent connection or background work."`; `remediation`: `"Do not add artificial disposal; The client allocates request closures but owns no persistent connection or background work. Verify tests/unit/core/graph/graph-mutation-client.test.ts."`; `siblingCleanup`: `"not-applicable"`; `specifier`: `"liteship/graph"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"pure-allocation"`; `disposal`: `"none"`; `operation`: `"createGraphQueryRefreshBase"`; `owner`: `"@liteship/core/graph"`; `postDispose`: `"not-applicable"`; `proof`: `"tests/unit/core/graph/graph-query.test.ts"`; `rationale`: `"The helper creates immutable refresh metadata and no active resource."`; `remediation`: `"Do not add artificial disposal; The helper creates immutable refresh metadata and no active resource. Verify tests/unit/core/graph/graph-query.test.ts."`; `siblingCleanup`: `"not-applicable"`; `specifier`: `"liteship/graph"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"Compositor.create"`; `owner`: `"@liteship/core/media"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/core/media/compositor.test.ts"`; `rationale`: `"The compositor owns reactive coordination, state delivery, and registered teardown."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/core/media/compositor.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/media"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"gc-owned-mutable"`; `disposal`: `"none"`; `operation`: `"createCompositorStatePool"`; `owner`: `"@liteship/core/media"`; `postDispose`: `"not-applicable"`; `proof`: `"tests/unit/core/media/compositor-pool.test.ts"`; `rationale`: `"The state pool is bounded reusable memory with no active external handle."`; `remediation`: `"Do not add artificial disposal; The state pool is bounded reusable memory with no active external handle. Verify tests/unit/core/media/compositor-pool.test.ts."`; `siblingCleanup`: `"not-applicable"`; `specifier`: `"liteship/media"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"pure-allocation"`; `disposal`: `"none"`; `operation`: `"createFrameSchedule"`; `owner`: `"@liteship/core/media"`; `postDispose`: `"not-applicable"`; `proof`: `"tests/unit/core/media/video.test.ts"`; `rationale`: `"The schedule is an immutable frame-coordinate projection and owns no clock, task, or external handle."`; `remediation`: `"Do not add artificial disposal; The schedule is an immutable frame-coordinate projection and owns no clock, task, or external handle. Verify tests/unit/core/media/video.test.ts."`; `siblingCleanup`: `"not-applicable"`; `specifier`: `"liteship/media"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createFrameBudget"`; `owner`: `"@liteship/core/media"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/core/media/frame-budget-runtime.test.ts"`; `rationale`: `"The frame budget owns an animation-frame loop and must cancel it directly."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/core/media/frame-budget-runtime.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/media"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"gc-owned-mutable"`; `disposal`: `"none"`; `operation`: `"createTokenBuffer"`; `owner`: `"@liteship/core/media"`; `postDispose`: `"not-applicable"`; `proof`: `"tests/unit/core/media/token-buffer.test.ts"`; `rationale`: `"The token buffer is bounded synchronous storage and owns no active resource."`; `remediation`: `"Do not add artificial disposal; The token buffer is bounded synchronous storage and owns no active resource. Verify tests/unit/core/media/token-buffer.test.ts."`; `siblingCleanup`: `"not-applicable"`; `specifier`: `"liteship/media"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `classification`: `"pure-allocation"`; `disposal`: `"none"`; `operation`: `"createVideoRenderer"`; `owner`: `"@liteship/core/media"`; `postDispose`: `"not-applicable"`; `proof`: `"tests/unit/core/media/video.test.ts"`; `rationale`: `"The renderer is a caller-driven async projection over a compositor and immutable frame schedule; it starts no independent task."`; `remediation`: `"Do not add artificial disposal; The renderer is a caller-driven async projection over a compositor and immutable frame schedule; it starts no independent task. Verify tests/unit/core/media/video.test.ts."`; `siblingCleanup`: `"not-applicable"`; `specifier`: `"liteship/media"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`, `"check/test-e2e"`\]; `classification`: `"gc-owned-mutable"`; `disposal`: `"none"`; `operation`: `"LLMAdapter.create"`; `owner`: `"@liteship/web"`; `postDispose`: `"not-applicable"`; `proof`: `"tests/unit/web/llm-adapter.test.ts"`; `rationale`: `"The adapter transforms caller-driven iteration and starts no independent task or connection."`; `remediation`: `"Do not add artificial disposal; The adapter transforms caller-driven iteration and starts no independent task or connection. Verify tests/unit/web/llm-adapter.test.ts."`; `siblingCleanup`: `"not-applicable"`; `specifier`: `"liteship/runtime"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`, `"check/test-e2e"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"SSE.create"`; `owner`: `"@liteship/web"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/liteship/facade-lifecycle-contract.test.ts"`; `rationale`: `"The client owns timers, one EventSource generation, buffers, and two delivery streams."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/liteship/facade-lifecycle-contract.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/runtime"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`, `"check/test-e2e"`\]; `classification`: `"gc-owned-mutable"`; `disposal`: `"none"`; `operation`: `"SlotRegistry.create"`; `owner`: `"@liteship/web"`; `postDispose`: `"not-applicable"`; `proof`: `"tests/unit/web/web-runtime-primitives.test.ts"`; `rationale`: `"The registry is inert storage; DOM observation is a separate operation returning its own disposer."`; `remediation`: `"Do not add artificial disposal; The registry is inert storage; DOM observation is a separate operation returning its own disposer. Verify tests/unit/web/web-runtime-primitives.test.ts."`; `siblingCleanup`: `"not-applicable"`; `specifier`: `"liteship/runtime"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`, `"check/test-e2e"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createAudioProcessor"`; `owner`: `"@liteship/web"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/liteship/facade-lifecycle-contract.test.ts"`; `rationale`: `"The processor owns a worklet node, graph connection, and bridge running state."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/liteship/facade-lifecycle-contract.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/runtime"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`, `"check/test-e2e"`\]; `classification`: `"pure-allocation"`; `disposal`: `"none"`; `operation`: `"createHtmlFragment"`; `owner`: `"@liteship/web"`; `postDispose`: `"not-applicable"`; `proof`: `"tests/unit/web/runtime-security-helpers.test.ts"`; `rationale`: `"The helper returns an inert document fragment and starts no observer or background work."`; `remediation`: `"Do not add artificial disposal; The helper returns an inert document fragment and starts no observer or background work. Verify tests/unit/web/runtime-security-helpers.test.ts."`; `siblingCleanup`: `"not-applicable"`; `specifier`: `"liteship/runtime"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`, `"check/test-e2e"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createPhysicalStateTracker"`; `owner`: `"@liteship/web"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/web/physical.test.ts"`; `rationale`: `"The tracker owns three document composition listeners; disposal removes every listener and clears the active IME state."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/web/physical.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/runtime"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`, `"check/test-e2e"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createWebCodecsCapture"`; `owner`: `"@liteship/web"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/web/web-capture.test.ts"`; `rationale`: `"The capture owns a VideoEncoder and mediabunny Output; terminal finalize and explicit disposal both release every host handle."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/web/web-capture.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/runtime"`; \}, \{ `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`, `"check/test-astro"`\]; `classification`: `"active-owned"`; `disposal`: `"dispose-async"`; `operation`: `"createLLMSession"`; `owner`: `"@liteship/astro/runtime"`; `postDispose`: `"inert"`; `proof`: `"tests/unit/astro/astro-runtime.test.ts"`; `rationale`: `"The session owns pooled render runtime state and queued delivery work."`; `remediation`: `"Dispose through dispose-async and verify tests/unit/astro/astro-runtime.test.ts; cleanup must remain idempotent, inert, and attempt-all."`; `siblingCleanup`: `"aggregate"`; `specifier`: `"liteship/astro"`; \}\]

### root

> `readonly` **root**: readonly \[\{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/test"`, `"check/hermetic"`, `"check/test-vite"`, `"check/test-astro"`\]; `example`: `"defineConfig(input)"`; `expertRoutes`: readonly \[`"liteship/reactive"`, `"liteship/compiler"`\]; `failureContract`: `"Invalid configuration is rejected before host projection."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"immutable-definition"`; `owner`: `"@liteship/core/authoring"`; `producer`: `"@liteship/core"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-inhabitation.test.ts"`, `"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/liteship/root-failure-contract.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/core/authoring, retry defineConfig(input), then run check/test and check/hermetic and check/test-vite and check/test-astro."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"defineConfig"`; `userStory`: `"Define the one immutable project configuration consumed by host integrations."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/test"`, `"check/hermetic"`\]; `example`: `"defineBoundary(input)"`; `expertRoutes`: readonly \[`"liteship/reactive"`, `"liteship/compiler"`\]; `failureContract`: `"Invalid thresholds or states are rejected at definition time."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"immutable-definition"`; `owner`: `"@liteship/core/authoring"`; `producer`: `"@liteship/core"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-inhabitation.test.ts"`, `"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/liteship/root-failure-contract.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/core/authoring, retry defineBoundary(input), then run check/test and check/hermetic."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"defineBoundary"`; `userStory`: `"Partition a continuous input into named application states."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/test"`, `"check/hermetic"`\]; `example`: `"defineQuantizer(boundary, options)"`; `expertRoutes`: readonly \[`"liteship/reactive"`, `"liteship/compiler"`\]; `failureContract`: `"Incomplete or invalid output maps are rejected before runtime allocation."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"immutable-definition"`; `owner`: `"@liteship/quantizer"`; `producer`: `"@liteship/quantizer"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-inhabitation.test.ts"`, `"tests/unit/liteship/facade-subpaths.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/quantizer, retry defineQuantizer(boundary, options), then run check/test and check/hermetic."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"defineQuantizer"`; `userStory`: `"Attach target outputs to a boundary without starting a runtime."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/test"`, `"check/hermetic"`\]; `example`: `"defineToken(input)"`; `expertRoutes`: readonly \[`"liteship/reactive"`, `"liteship/compiler"`\]; `failureContract`: `"Invalid token identity or values are rejected at definition time."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"immutable-definition"`; `owner`: `"@liteship/core/authoring"`; `producer`: `"@liteship/core"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-inhabitation.test.ts"`, `"tests/unit/liteship/facade-subpaths.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/core/authoring, retry defineToken(input), then run check/test and check/hermetic."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"defineToken"`; `userStory`: `"Define one content-addressed design token."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/test"`, `"check/hermetic"`\]; `example`: `"defineTheme(input)"`; `expertRoutes`: readonly \[`"liteship/reactive"`, `"liteship/compiler"`\]; `failureContract`: `"Invalid variants or token references are rejected at definition time."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"immutable-definition"`; `owner`: `"@liteship/core/authoring"`; `producer`: `"@liteship/core"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-inhabitation.test.ts"`, `"tests/unit/liteship/facade-subpaths.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/core/authoring, retry defineTheme(input), then run check/test and check/hermetic."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"defineTheme"`; `userStory`: `"Define named token variants as one immutable theme."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/test"`, `"check/hermetic"`\]; `example`: `"defineStyle(input)"`; `expertRoutes`: readonly \[`"liteship/reactive"`, `"liteship/compiler"`\]; `failureContract`: `"Invalid declarations or state ownership are rejected at definition time."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"immutable-definition"`; `owner`: `"@liteship/core/authoring"`; `producer`: `"@liteship/core"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-inhabitation.test.ts"`, `"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/liteship/root-failure-contract.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/core/authoring, retry defineStyle(input), then run check/test and check/hermetic."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"defineStyle"`; `userStory`: `"Define base and state-layer declarations against one boundary."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/test"`, `"check/hermetic"`, `"check/journey"`, `"check/test-e2e"`\]; `example`: `"defineAdaptive(spec)"`; `expertRoutes`: readonly \[`"liteship/reactive"`, `"liteship/motion"`, `"liteship/compiler"`, `"liteship/astro"`, `"liteship/vite"`\]; `failureContract`: `"Lowering rejects invalid definitions and explanation follows the live quantizer contract."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"immutable-definition"`; `owner`: `"liteship"`; `producer`: `"./adaptive.js"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-inhabitation.test.ts"`, `"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/liteship/root-failure-contract.test.ts"`, `"tests/property/adaptive-lowering-equivalence.prop.test.ts"`, `"tests/e2e/astro-directives.e2e.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at liteship, retry defineAdaptive(spec), then run check/test and check/hermetic and check/journey and check/test-e2e."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"defineAdaptive"`; `userStory`: `"Define adaptive behavior, apply its attributes, inspect state, and emit its compiled plan."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/test"`, `"check/hermetic"`\]; `example`: `"schema.struct(fields)"`; `expertRoutes`: readonly \[`"liteship/schema"`\]; `failureContract`: `"Decode returns structured issues instead of accepting malformed data."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"immutable-definition"`; `owner`: `"@liteship/core/schema"`; `producer`: `"@liteship/core"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-inhabitation.test.ts"`, `"tests/unit/liteship/facade-subpaths.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/core/schema, retry schema.struct(fields), then run check/test and check/hermetic."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"schema"`; `userStory`: `"Describe and decode typed data at a semantic boundary."`; \}, \{ `audience`: `"operator"`; `category`: `"inspection"`; `checkIds`: readonly \[`"check/test"`, `"check/hermetic"`\]; `example`: `"explainDiagnostic(code)"`; `expertRoutes`: readonly \[`"liteship/evidence"`\]; `failureContract`: `"Unknown codes return no invented explanation."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"pure-reader"`; `owner`: `"@liteship/error"`; `producer`: `"@liteship/error"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-inhabitation.test.ts"`, `"tests/unit/liteship/facade-subpaths.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/error, retry explainDiagnostic(code), then run check/test and check/hermetic."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"explainDiagnostic"`; `userStory`: `"Turn a stable diagnostic code into meaning and remediation."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/typecheck"`, `"check/hermetic"`, `"check/test-vite"`, `"check/test-astro"`\]; `example`: `"Config"`; `expertRoutes`: readonly \[`"liteship/reactive"`, `"liteship/compiler"`\]; `failureContract`: `"Type checking rejects incompatible configuration shapes."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"compile-time-only"`; `owner`: `"@liteship/core/authoring"`; `producer`: `"@liteship/core"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/meta/api-surface.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/core/authoring, retry Config, then run check/typecheck and check/hermetic and check/test-vite and check/test-astro."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"Config"`; `userStory`: `"Annotate an authored project configuration."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/typecheck"`, `"check/hermetic"`\]; `example`: `"Boundary<'viewport.width', readonly ['mobile', 'desktop']>"`; `expertRoutes`: readonly \[`"liteship/reactive"`, `"liteship/compiler"`\]; `failureContract`: `"Type checking preserves the input and state vocabulary."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"compile-time-only"`; `owner`: `"@liteship/core/authoring"`; `producer`: `"@liteship/core"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/meta/api-surface.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/core/authoring, retry Boundary<'viewport.width', readonly ['mobile', 'desktop']>, then run check/typecheck and check/hermetic."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"Boundary"`; `userStory`: `"Annotate a named-state boundary definition."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/typecheck"`, `"check/hermetic"`\]; `example`: `"Quantizer<Boundary>"`; `expertRoutes`: readonly \[`"liteship/schema"`\]; `failureContract`: `"Type checking rejects state or output mismatches."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"compile-time-only"`; `owner`: `"@liteship/core/schema"`; `producer`: `"@liteship/core"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/meta/api-surface.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/core/schema, retry Quantizer<Boundary>, then run check/typecheck and check/hermetic."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"Quantizer"`; `userStory`: `"Annotate the structural mapping from boundary states to outputs."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/typecheck"`, `"check/hermetic"`\]; `example`: `"Token<'brand-accent'>"`; `expertRoutes`: readonly \[`"liteship/reactive"`, `"liteship/compiler"`\]; `failureContract`: `"Type checking preserves the token value contract."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"compile-time-only"`; `owner`: `"@liteship/core/authoring"`; `producer`: `"@liteship/core"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/meta/api-surface.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/core/authoring, retry Token<'brand-accent'>, then run check/typecheck and check/hermetic."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"Token"`; `userStory`: `"Annotate a content-addressed token definition."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/typecheck"`, `"check/hermetic"`\]; `example`: `"Theme<readonly ['default', 'dark']>"`; `expertRoutes`: readonly \[`"liteship/reactive"`, `"liteship/compiler"`\]; `failureContract`: `"Type checking preserves variant names and token values."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"compile-time-only"`; `owner`: `"@liteship/core/authoring"`; `producer`: `"@liteship/core"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/meta/api-surface.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/core/authoring, retry Theme<readonly ['default', 'dark']>, then run check/typecheck and check/hermetic."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"Theme"`; `userStory`: `"Annotate named token variants."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/typecheck"`, `"check/hermetic"`\]; `example`: `"Style<Boundary>"`; `expertRoutes`: readonly \[`"liteship/reactive"`, `"liteship/compiler"`\]; `failureContract`: `"Type checking binds style states to the boundary vocabulary."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"compile-time-only"`; `owner`: `"@liteship/core/authoring"`; `producer`: `"@liteship/core"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/meta/api-surface.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/core/authoring, retry Style<Boundary>, then run check/typecheck and check/hermetic."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"Style"`; `userStory`: `"Annotate state-aware style declarations."`; \}, \{ `audience`: `"application-author"`; `category`: `"authoring"`; `checkIds`: readonly \[`"check/typecheck"`, `"check/hermetic"`, `"check/journey"`, `"check/test-e2e"`\]; `example`: `"Adaptive"`; `expertRoutes`: readonly \[\]; `failureContract`: `"Type checking keeps attrs, explanation, and plan outputs coherent."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"compile-time-only"`; `owner`: `"@liteship/core"`; `producer`: `"@liteship/core"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/meta/api-surface.test.ts"`, `"tests/property/adaptive-lowering-equivalence.prop.test.ts"`, `"tests/e2e/astro-directives.e2e.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/core, retry Adaptive, then run check/typecheck and check/hermetic and check/journey and check/test-e2e."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"Adaptive"`; `userStory`: `"Annotate the flagship define, apply, and inspect aggregate."`; \}, \{ `audience`: `"operator"`; `category`: `"inspection"`; `checkIds`: readonly \[`"check/typecheck"`, `"check/hermetic"`\]; `example`: `"DiagnosticCode"`; `expertRoutes`: readonly \[`"liteship/evidence"`\]; `failureContract`: `"Type checking rejects unknown diagnostic identifiers."`; `failureProof`: `null`; `kind`: `"root"`; `lifecycle`: `"compile-time-only"`; `owner`: `"@liteship/error"`; `producer`: `"@liteship/error"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/meta/api-surface.test.ts"`\]; `relatedInvariant`: `"INV-FACADE-EXPORT-BUDGET"`; `remediation`: `"Correct the input at @liteship/error, retry DiagnosticCode, then run check/typecheck and check/hermetic."`; `replacement`: `"none"`; `specifier`: `"liteship"`; `stability`: `"stable"`; `surfaceClass`: `"paved-road"`; `symbol`: `"DiagnosticCode"`; `userStory`: `"Annotate a stable code accepted by diagnostic inspection."`; \}\]

### subpaths

> `readonly` **subpaths**: readonly \[\{ `audience`: `"application-author"`; `category`: `"schema"`; `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `dependencyCost`: `"pure core schema kernel"`; `example`: `"schema.struct(fields)"`; `expertRoutes`: readonly \[`"liteship/schema"`\]; `failureContract`: `"Malformed data returns structured decode issues."`; `failureProof`: `null`; `kind`: `"subpath"`; `lifecycle`: `"immutable definitions and pure decoders"`; `owner`: `"@liteship/core/schema"`; `packedProof`: `"check/hermetic:runtime-import+node16+bundler"`; `producer`: `"@liteship/core/schema"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`\]; `reason`: `"Schema authoring is a coherent expert domain beyond the first adaptive feature."`; `relatedInvariant`: `"INV-CONSUMER-SUBPATH-CLOSURE"`; `remediation`: `"Correct the input at @liteship/core/schema, retry schema.struct(fields), then run check/hermetic and check/test."`; `replacement`: `"none"`; `role`: `"schema"`; `specifier`: `"liteship/schema"`; `stability`: `"stable"`; `surfaceClass`: `"advanced-module"`; `symbol`: `"schema"`; `userStory`: `"Define, decode, and project transport-agnostic schemas."`; \}, \{ `audience`: `"application-author"`; `category`: `"reactive-runtime"`; `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `dependencyCost`: `"stateful core and quantizer runtime"`; `example`: `"createCell(initial)"`; `expertRoutes`: readonly \[`"liteship/reactive"`\]; `failureContract`: `"Disposed resources stop work and invalid state transitions fail loudly."`; `failureProof`: `null`; `kind`: `"subpath"`; `lifecycle`: `"owned resources require disposal"`; `owner`: `"@liteship/core/reactive"`; `packedProof`: `"check/hermetic:runtime-import+node16+bundler"`; `producer`: `"@liteship/core/reactive"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/core/reactive/runtime-coordinator.test.ts"`, `"tests/unit/core/reactive/state-cell.test.ts"`, `"tests/unit/core/reactive/cell.test.ts"`, `"tests/unit/core/reactive/dirty-flags-runtime.test.ts"`, `"tests/unit/core/reactive/lifetime.test.ts"`, `"tests/unit/core/reactive/disposed-write-inert.test.ts"`, `"tests/unit/quantizer/animated-quantizer.test.ts"`, `"tests/unit/core/reactive/signal.test.ts"`, `"tests/unit/core/reactive/store.test.ts"`\]; `reason`: `"Runtime allocation is intentionally distinct from immutable root authoring."`; `relatedInvariant`: `"INV-CONSUMER-SUBPATH-CLOSURE"`; `remediation`: `"Correct the input at @liteship/core/reactive, retry createCell(initial), then run check/hermetic and check/test."`; `replacement`: `"none"`; `role`: `"reactive-runtime"`; `specifier`: `"liteship/reactive"`; `stability`: `"stable"`; `surfaceClass`: `"advanced-module"`; `symbol`: `"createCell"`; `userStory`: `"Allocate and dispose cells, signals, stores, lifetimes, and live quantizers."`; \}, \{ `audience`: `"application-author"`; `category`: `"motion"`; `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `dependencyCost`: `"motion kernels"`; `example`: `"createTimeline(boundary, options)"`; `expertRoutes`: readonly \[`"liteship/motion"`\]; `failureContract`: `"Unsupported or invalid motion intent is refused before execution."`; `failureProof`: `null`; `kind`: `"subpath"`; `lifecycle`: `"timeline resources require disposal"`; `owner`: `"@liteship/core/motion"`; `packedProof`: `"check/hermetic:runtime-import+node16+bundler"`; `producer`: `"@liteship/core/motion"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/core/motion/blend-tree.test.ts"`, `"tests/unit/liteship/facade-lifecycle-contract.test.ts"`\]; `reason`: `"Motion is an expert capability with its own lifecycle and vocabulary."`; `relatedInvariant`: `"INV-CONSUMER-SUBPATH-CLOSURE"`; `remediation`: `"Correct the input at @liteship/core/motion, retry createTimeline(boundary, options), then run check/hermetic and check/test."`; `replacement`: `"none"`; `role`: `"motion"`; `specifier`: `"liteship/motion"`; `stability`: `"stable"`; `surfaceClass`: `"advanced-module"`; `symbol`: `"createTimeline"`; `userStory`: `"Define and execute transitions, timelines, easing, reveal, and stagger behavior."`; \}, \{ `audience`: `"application-author"`; `category`: `"document-graph"`; `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `dependencyCost`: `"graph and evidence kernels"`; `example`: `"DAG.empty()"`; `expertRoutes`: readonly \[`"liteship/graph"`\]; `failureContract`: `"Invalid nodes, patches, or receipts are rejected before application."`; `failureProof`: `null`; `kind`: `"subpath"`; `lifecycle`: `"immutable sealed graphs plus explicit clients"`; `owner`: `"@liteship/core/graph"`; `packedProof`: `"check/hermetic:runtime-import+node16+bundler"`; `producer`: `"@liteship/core/graph"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/core/graph/graph-mutation-client.test.ts"`, `"tests/unit/core/graph/graph-query.test.ts"`\]; `reason`: `"Graph mutation is an advanced engine workflow, not first-hour authoring."`; `relatedInvariant`: `"INV-CONSUMER-SUBPATH-CLOSURE"`; `remediation`: `"Correct the input at @liteship/core/graph, retry DAG.empty(), then run check/hermetic and check/test."`; `replacement`: `"none"`; `role`: `"document-graph"`; `specifier`: `"liteship/graph"`; `stability`: `"stable"`; `surfaceClass`: `"advanced-module"`; `symbol`: `"DAG"`; `userStory`: `"Seal, validate, patch, query, and replay the document graph."`; \}, \{ `audience`: `"application-author"`; `category`: `"media-runtime"`; `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `dependencyCost`: `"media and compositor runtime"`; `example`: `"Compositor.create(options)"`; `expertRoutes`: readonly \[`"liteship/media"`\]; `failureContract`: `"Invalid media inputs and exhausted budgets are surfaced explicitly."`; `failureProof`: `null`; `kind`: `"subpath"`; `lifecycle`: `"owned buffers and renderers require disposal"`; `owner`: `"@liteship/core/media"`; `packedProof`: `"check/hermetic:runtime-import+node16+bundler"`; `producer`: `"@liteship/core/media"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/core/media/compositor.test.ts"`, `"tests/unit/core/media/compositor-pool.test.ts"`, `"tests/unit/core/media/video.test.ts"`, `"tests/unit/core/media/frame-budget-runtime.test.ts"`, `"tests/unit/core/media/token-buffer.test.ts"`\]; `reason`: `"Media processing has runtime and performance costs unsuitable for root."`; `relatedInvariant`: `"INV-CONSUMER-SUBPATH-CLOSURE"`; `remediation`: `"Correct the input at @liteship/core/media, retry Compositor.create(options), then run check/hermetic and check/test."`; `replacement`: `"none"`; `role`: `"media-runtime"`; `specifier`: `"liteship/media"`; `stability`: `"stable"`; `surfaceClass`: `"advanced-module"`; `symbol`: `"Compositor"`; `userStory`: `"Resolve responsive media and run compositor, audio, video, and frame-budget paths."`; \}, \{ `audience`: `"operator"`; `category`: `"evidence-and-quality"`; `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `dependencyCost`: `"pure evidence kernels"`; `example`: `"inspectReceipt(receipt)"`; `expertRoutes`: readonly \[`"liteship/evidence"`\]; `failureContract`: `"Invalid chains, addresses, and capability decisions are refused with structured evidence."`; `failureProof`: `null`; `kind`: `"subpath"`; `lifecycle`: `"pure readers and immutable receipts"`; `owner`: `"@liteship/core/evidence"`; `packedProof`: `"check/hermetic:runtime-import+node16+bundler"`; `producer`: `"@liteship/core/evidence"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`\]; `reason`: `"Receipts and tier policy are expert inspection surfaces."`; `relatedInvariant`: `"INV-CONSUMER-SUBPATH-CLOSURE"`; `remediation`: `"Correct the input at @liteship/core/evidence, retry inspectReceipt(receipt), then run check/hermetic and check/test."`; `replacement`: `"none"`; `role`: `"evidence-and-quality"`; `specifier`: `"liteship/evidence"`; `stability`: `"stable"`; `surfaceClass`: `"advanced-module"`; `symbol`: `"inspectReceipt"`; `userStory`: `"Inspect receipts, diagnostics, quality tiers, capabilities, and addressed evidence."`; \}, \{ `audience`: `"application-author"`; `category`: `"projection-compiler"`; `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `dependencyCost`: `"compiler kernels"`; `example`: `"CSSCompiler.compile(boundary, input)"`; `expertRoutes`: readonly \[`"liteship/compiler"`\]; `failureContract`: `"CSS state keys outside the boundary are omitted and emit the registered compiler/css/unknown-state-key diagnostic."`; `failureProof`: \{ `importSource`: `"../../../packages/liteship/src/compiler.js"`; `observation`: \{ `code`: `"compiler/css/unknown-state-key"`; `kind`: `"diagnostic-and-output-omission"`; `outputField`: `"raw"`; \}; `operation`: `"CSSCompiler.compile"`; `test`: `"tests/unit/liteship/facade-failure-contract.test.ts::liteship/compiler failure contract > CSSCompiler.compile omits an unknown state and emits its registered diagnostic"`; \}; `kind`: `"subpath"`; `lifecycle`: `"pure compilation"`; `owner`: `"@liteship/compiler"`; `packedProof`: `"check/hermetic:runtime-import+node16+bundler"`; `producer`: `"@liteship/compiler"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/liteship/facade-failure-contract.test.ts"`\]; `reason`: `"Projection targets are advanced escape hatches behind the default Adaptive plan."`; `relatedInvariant`: `"INV-CONSUMER-SUBPATH-CLOSURE"`; `remediation`: `"Correct the input at @liteship/compiler, retry CSSCompiler.compile(boundary, input), then run check/hermetic and check/test."`; `replacement`: `"none"`; `role`: `"projection-compiler"`; `specifier`: `"liteship/compiler"`; `stability`: `"stable"`; `surfaceClass`: `"advanced-module"`; `symbol`: `"CSSCompiler"`; `userStory`: `"Compile definitions into CSS, shader, accessibility, AI, and motion targets."`; \}, \{ `audience`: `"application-author"`; `category`: `"browser-runtime"`; `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`, `"check/test-e2e"`\]; `dependencyCost`: `"browser DOM runtime"`; `example`: `"Morph.morph(input, input)"`; `expertRoutes`: readonly \[`"liteship/runtime"`\]; `failureContract`: `"Unsafe URLs, invalid patches, and broken resumptions fail closed."`; `failureProof`: `null`; `kind`: `"subpath"`; `lifecycle`: `"connections and observers require disposal"`; `owner`: `"@liteship/web"`; `packedProof`: `"check/hermetic:runtime-import+node16+bundler"`; `producer`: `"@liteship/web"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/web/llm-adapter.test.ts"`, `"tests/unit/liteship/facade-lifecycle-contract.test.ts"`, `"tests/unit/web/web-runtime-primitives.test.ts"`, `"tests/unit/web/runtime-security-helpers.test.ts"`, `"tests/unit/web/physical.test.ts"`, `"tests/unit/web/web-capture.test.ts"`\]; `reason`: `"Browser runtime code must never load through the host-free root."`; `relatedInvariant`: `"INV-CONSUMER-SUBPATH-CLOSURE"`; `remediation`: `"Correct the input at @liteship/web, retry Morph.morph(input, input), then run check/hermetic and check/test and check/test-e2e."`; `replacement`: `"none"`; `role`: `"browser-runtime"`; `specifier`: `"liteship/runtime"`; `stability`: `"stable"`; `surfaceClass`: `"advanced-module"`; `symbol`: `"Morph"`; `userStory`: `"Apply streaming, morphing, recovery, integrity, and browser runtime behavior."`; \}, \{ `audience`: `"host-integrator"`; `category`: `"astro-host"`; `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`, `"check/test-astro"`\]; `dependencyCost`: `"optional Astro peer and host adapter"`; `example`: `"adaptiveAttrs(input)"`; `expertRoutes`: readonly \[`"liteship/astro"`\]; `failureContract`: `"Invalid host configuration fails during integration setup or build."`; `failureProof`: `null`; `kind`: `"subpath"`; `lifecycle`: `"host integration lifecycle"`; `owner`: `"@liteship/astro"`; `packedProof`: `"check/hermetic:runtime-import+node16+bundler"`; `producer`: `"@liteship/astro"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`, `"tests/unit/astro/astro-runtime.test.ts"`\]; `reason`: `"Astro ownership and peer cost require an explicit host subpath."`; `relatedInvariant`: `"INV-CONSUMER-SUBPATH-CLOSURE"`; `remediation`: `"Correct the input at @liteship/astro, retry adaptiveAttrs(input), then run check/hermetic and check/test and check/test-astro."`; `replacement`: `"none"`; `role`: `"astro-host"`; `specifier`: `"liteship/astro"`; `stability`: `"stable"`; `surfaceClass`: `"advanced-module"`; `symbol`: `"adaptiveAttrs"`; `userStory`: `"Install LiteShip into Astro and apply Adaptive attributes and server projections."`; \}, \{ `audience`: `"host-integrator"`; `category`: `"vite-host"`; `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`, `"check/test-vite"`\]; `dependencyCost`: `"optional Vite peer and host plugin"`; `example`: `"plugin(options)"`; `expertRoutes`: readonly \[`"liteship/vite"`\]; `failureContract`: `"Invalid directives and configuration produce stable build diagnostics."`; `failureProof`: `null`; `kind`: `"subpath"`; `lifecycle`: `"host plugin lifecycle"`; `owner`: `"@liteship/vite"`; `packedProof`: `"check/hermetic:runtime-import+node16+bundler"`; `producer`: `"@liteship/vite"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`\]; `reason`: `"Vite ownership and peer cost require an explicit host subpath."`; `relatedInvariant`: `"INV-CONSUMER-SUBPATH-CLOSURE"`; `remediation`: `"Correct the input at @liteship/vite, retry plugin(options), then run check/hermetic and check/test and check/test-vite."`; `replacement`: `"none"`; `role`: `"vite-host"`; `specifier`: `"liteship/vite"`; `stability`: `"stable"`; `surfaceClass`: `"advanced-module"`; `symbol`: `"plugin"`; `userStory`: `"Install LiteShip into Vite and compile directive and virtual-module projections."`; \}, \{ `audience`: `"package-author"`; `category`: `"test-tooling"`; `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`, `"check/capsule-verify"`\]; `dependencyCost`: `"test-only harness code and fast-check peer"`; `example`: `"generatePureTransform(spec)"`; `expertRoutes`: readonly \[`"liteship/testing"`\]; `failureContract`: `"Invalid harness declarations and stale fleet projections fail deterministically."`; `failureProof`: `null`; `kind`: `"subpath"`; `lifecycle`: `"test process only"`; `owner`: `"@liteship/core/harness"`; `packedProof`: `"check/hermetic:runtime-import+node16+bundler"`; `producer`: `"@liteship/core/harness"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`\]; `reason`: `"Proof generators and fleet metadata are a public package-author contract that must remain isolated from production root imports."`; `relatedInvariant`: `"INV-CONSUMER-SUBPATH-CLOSURE"`; `remediation`: `"Correct the input at @liteship/core/harness, retry generatePureTransform(spec), then run check/hermetic and check/test and check/capsule-verify."`; `replacement`: `"none"`; `role`: `"test-tooling"`; `specifier`: `"liteship/testing"`; `stability`: `"stable"`; `surfaceClass`: `"advanced-module"`; `symbol`: `"generatePureTransform"`; `userStory`: `"Generate proof harnesses and inspect the installed fleet roster without exposing test tooling on the production root."`; \}, \{ `audience`: `"application-author"`; `category`: `"migration"`; `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`\]; `dependencyCost`: `"compiler parser adapters"`; `example`: `"fromMediaQueries(css)"`; `expertRoutes`: readonly \[`"liteship/migrate"`\]; `failureContract`: `"Unrepresentable source is refused with stable diagnostics and no fabricated definition."`; `failureProof`: `null`; `kind`: `"subpath"`; `lifecycle`: `"pure migration"`; `owner`: `"@liteship/compiler/migrate"`; `packedProof`: `"check/hermetic:runtime-import+node16+bundler"`; `producer`: `"@liteship/compiler/migrate"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`\]; `reason`: `"Migration grammar is an explicit expert boundary, not framework ontology."`; `relatedInvariant`: `"INV-CONSUMER-SUBPATH-CLOSURE"`; `remediation`: `"Correct the input at @liteship/compiler/migrate, retry fromMediaQueries(css), then run check/hermetic and check/test."`; `replacement`: `"none"`; `role`: `"migration"`; `specifier`: `"liteship/migrate"`; `stability`: `"stable"`; `surfaceClass`: `"advanced-module"`; `symbol`: `"fromMediaQueries"`; `userStory`: `"Translate supported external syntax into ordinary LiteShip definitions."`; \}, \{ `audience`: `"application-author"`; `category`: `"generated-ui"`; `checkIds`: readonly \[`"check/hermetic"`, `"check/test"`, `"check/journey"`\]; `dependencyCost`: `"pure generated-UI catalog and renderer"`; `example`: `"defineComponentCatalog(input)"`; `expertRoutes`: readonly \[`"liteship/genui"`\]; `failureContract`: `"Unknown components, props, or invalid generated trees are refused before rendering."`; `failureProof`: `null`; `kind`: `"subpath"`; `lifecycle`: `"immutable catalog and pure validation/rendering"`; `owner`: `"@liteship/genui"`; `packedProof`: `"check/journey:one-install-runtime-reference-identity"`; `producer`: `"@liteship/genui"`; `proofRefs`: readonly \[`"tests/unit/liteship/facade-subpaths.test.ts"`\]; `reason`: `"Generated UI is a documented product capability that deserves a discoverable facade subpath."`; `relatedInvariant`: `"INV-CONSUMER-SUBPATH-CLOSURE"`; `remediation`: `"Correct the input at @liteship/genui, retry defineComponentCatalog(input), then run check/hermetic and check/test and check/journey."`; `replacement`: `"none"`; `role`: `"generated-ui"`; `specifier`: `"liteship/genui"`; `stability`: `"stable"`; `surfaceClass`: `"advanced-module"`; `symbol`: `"defineComponentCatalog"`; `userStory`: `"Define a trusted component catalog, validate generated UI, and render it without raw package discovery."`; \}\]
