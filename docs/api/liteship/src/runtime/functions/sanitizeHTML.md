[**LiteShip**](../../../../README.md)

***

[LiteShip](../../../../README.md) / [liteship/src/runtime](../README.md) / sanitizeHTML

# Function: sanitizeHTML()

> **sanitizeHTML**(`html`): `string`

Defined in: web/dist/security/html-trust.d.ts:65

Convenience wrapper that always applies the `sanitized-html` policy.
Use when a caller just wants the "clean the markup" behaviour without
threading options.

## Parameters

### html

`string`

## Returns

`string`
