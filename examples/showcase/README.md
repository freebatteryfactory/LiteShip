# czap showcase

Feature showcase for workers, WASM, GPU, stream, and LLM client directives.

When installing from npm (outside the monorepo), pin `@czap/*` packages at `^0.4.0`.

```bash
pnpm install
pnpm dev
```
