# czap Remotion demo

Remotion composition driven by czap boundaries and quantizer outputs.

When installing from npm (outside the monorepo), pin `@czap/*` packages at `^0.4.0`.

```bash
pnpm install
pnpm dev
```
