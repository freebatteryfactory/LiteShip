# czap tutorial

Step-by-step Astro tutorial for boundaries, tokens, streaming, and LLM directives.

When installing from npm (outside the monorepo), pin `@czap/*` packages at `^0.2.0`.

```bash
pnpm install
pnpm dev
```
