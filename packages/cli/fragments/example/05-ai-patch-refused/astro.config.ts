import { defineConfig } from 'astro/config';

// This example is about the @liteship/core AI-apply seam (validate → witness → apply). It
// uses no boundaries or client directives, so no liteship integration is wired — the point
// is the pure, host-authorable safety property, rendered at build time.
export default defineConfig({});
